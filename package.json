{
  "name": "projectmma",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w apps/backend",
    "install:all": "npm install --workspaces"
  }
}
